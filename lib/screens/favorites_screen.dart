import 'package:flutter/material.dart';

// TODO: Build favorites screen.

class FavoritesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Favorites!"),
    );
  }
}
